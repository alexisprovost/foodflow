import useDocumentTitle from "../hooks/useDocumentTitle";

const Cart = () => {
	useDocumentTitle("Cart");

	return (
		<div className="login-page">
			<h1>Cart</h1>
		</div>
	);
};

export default Cart;